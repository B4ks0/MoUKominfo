document.addEventListener('DOMContentLoaded', function () {
    // Initialize SweetAlert2 can be used elsewhere as needed (already imported via CDN)
    window.Swal = Swal;

    // Chart.js is initialized in dashboard.html where needed
});
